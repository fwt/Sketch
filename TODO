
----------------
   TODO
----------------

 - improve SVG import
 - create generic SVG object to access common properties
 - start collaboration only when the same document is loaded (unique ID for each document)
 - undo/redo for actions of other collaborative users
 - selecting and deleting objects
 - rotating and resizing objects
 - set and show image borders
 - improve color selection dialog
 - showing and editing object attributes
 - show other collaborative users online
 - add library with predefined objects





Next Steps:

- rewrite DOM2Obj function
  a) set properties by hand
  b) semi-automatical object reading and writing
- document ID
- element ID
- NEW message -> objectID, parentID, ParentPos, SVG code (incl. objectID)
- CHANGE message -> objectID, deltaX, deltaY, deltaWidth, deltaHeight
- DELETE message -> objectID
- Undo stack UNDO REDO (-> action, tool, obj)
- XML to Objects (Gedanken machen, Reihenfolge)
